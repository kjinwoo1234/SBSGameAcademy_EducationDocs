# C 포인터

포인터는 C 언어의 핵심 개념으로, 메모리 주소를 다루기 위한 도구입니다.  
배열, 함수, 동적 메모리와 모두 연결되므로 반드시 깊게 이해해야 합니다.

## 1) 주소와 포인터
- `&변수`: 변수의 주소
- `*포인터`: 그 주소가 가리키는 실제 값(역참조)

```c
int n = 10;
int* p = &n;
printf("주소: %p\n", (void*)p);
printf("값: %d\n", *p);
```

## 2) 포인터를 통한 값 변경
```c
int n = 10;
int* p = &n;
*p = 30;
printf("%d\n", n); // 30
```

## 3) 포인터와 배열 관계
배열 이름은 첫 요소의 주소처럼 동작합니다.

```c
int arr[3] = {10, 20, 30};
int* p = arr;
printf("%d\n", *(p + 1)); // 20
printf("%d\n", p[2]);     // 30
```

## 4) NULL 포인터
아직 유효한 주소를 가리키지 않을 때 `NULL`로 초기화해 안전성을 높입니다.

```c
int* p = NULL;
```

## 5) 자주 하는 실수
- 초기화하지 않은 포인터 역참조
- 해제된 메모리를 계속 사용하는 댕글링 포인터
- 잘못된 포인터 연산으로 범위를 벗어나는 접근

## 6) 연습 문제
1. 포인터를 이용해 두 정수의 값을 교환하세요.
2. 배열을 포인터로 순회하여 합계를 계산하세요.
3. `NULL` 검사 후 역참조하도록 안전 코드를 작성하세요.

---

[상위 문서로 돌아가기](./README.md)
