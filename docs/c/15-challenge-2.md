# Chapter 15 도전! 프로그래밍 2

## 학습 목표
- 콘솔 제어와 함수 분할을 실전 문제에 적용한다.
- 작은 프로젝트 단위로 기능을 조립해본다.

## 세부 주제
### 15-1 커서 이동 함수
- 콘솔 좌표 제어 기본 아이디어

### 15-2 글자 색상 변경 함수
- 출력 스타일 변경 함수 분리

### 15-3 글자 전부 지우는 함수
- 화면 초기화와 출력 갱신

### 15-4 머드게임 만들기
- 입력/출력/상태 관리 결합

## 실습 체크리스트
- 3개 이상의 유틸 함수로 콘솔 미니게임을 구성한다.

## 본문

### 15-1 커서 이동 함수
- 콘솔 좌표 이동 함수(`gotoxy`)를 만들면 UI 표현이 좋아집니다.
- 화면을 다시 그릴 위치를 제어할 수 있어 게임 느낌을 만들기 좋습니다.

### 15-2 글자 색상 변경 함수
- 색상 출력 함수(`setColor`)를 분리하면 경고/상태 메시지를 직관적으로 표현할 수 있습니다.

### 15-3 글자 전부 지우는 함수
- 화면 지우기 함수(`clearScreen`)를 통일해 쓰면 프레임 갱신이 쉬워집니다.

### 15-4 머드게임 만들기
- 입력 처리, 상태 업데이트, 렌더링을 함수로 분리하면 유지보수가 쉬워집니다.

권장 함수 분리:
- `init_game()`: 초기화
- `handle_input()`: 입력 처리
- `update_game()`: 상태 갱신
- `render_game()`: 화면 출력

예시 코드(구조 템플릿):
```c
#include <stdio.h>

void init_game(void) { printf("init\n"); }
void handle_input(void) { /* 입력 처리 */ }
void update_game(void) { /* 상태 갱신 */ }
void render_game(void) { /* 화면 출력 */ }

int main(void) {
    init_game();
    for (int frame = 0; frame < 3; frame++) {
        handle_input();
        update_game();
        render_game();
    }
    return 0;
}
```

연습문제:
1. 커서 이동 + 색상 출력 함수 2개를 만들어 상태판을 출력해보세요.
2. 플레이어 HP와 EXP를 갱신하는 함수를 분리해보세요.

정답 포인트:
- 전역변수 최소화
- 기능별 함수 분리
- 입력/업데이트/출력 루프 구조 유지

---

[상위 문서로 돌아가기](./README.md)
