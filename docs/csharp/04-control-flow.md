# C# 제어문

## 핵심
- 조건문: `if`, `else if`, `else`, `switch`
- 반복문: `for`, `while`
- 흐름 제어: `break`, `continue`

## 예제
```csharp
for (int i = 1; i <= 10; i++)
{
    if (i == 3) continue;
    if (i == 6) break;
    Console.WriteLine(i);
}
```

---

[상위 문서로 돌아가기](./README.md)
