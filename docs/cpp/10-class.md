# C++ 클래스

클래스는 데이터(멤버 변수)와 기능(멤버 함수)을 하나로 묶는 객체지향 핵심 문법입니다.  
캡슐화를 통해 잘못된 상태 변경을 막고 유지보수를 쉽게 만듭니다.

## 학습 목표
- 클래스 기본 구조(멤버 변수/멤버 함수)를 작성할 수 있다.
- 접근 지정자(`private`, `public`, `protected`) 차이를 설명할 수 있다.
- 생성자와 `const` 멤버 함수의 목적을 이해할 수 있다.

## 핵심
- 캡슐화: 데이터 보호 + 인터페이스 분리
- 생성자 초기화 리스트: 효율적 초기화
- `const` 멤버 함수: 상태 변경 금지 보장

## 1) 기본 구조
```cpp
class Player {
private:
    int hp;
public:
    Player(int value) : hp(value) {}
    void Hit(int damage) { hp -= damage; }
    int GetHp() const { return hp; }
};
```

## 2) 접근 지정자
- `private`: 클래스 내부에서만 접근
- `public`: 외부 접근 허용
- `protected`: 상속 관계에서 접근 가능

## 3) 생성자와 초기화 리스트
생성자 초기화 리스트는 멤버 초기화의 권장 방식입니다.

## 4) `const` 멤버 함수
객체 상태를 바꾸지 않는 함수임을 보장합니다.

## 실수 포인트
- 멤버 변수를 `public`으로 열어 상태가 무분별하게 변경된다.
- 생성자에서 초기화 리스트를 쓰지 않아 불필요한 대입이 발생한다.
- `const`가 필요한 함수에 누락되어 호출 제약이 생긴다.

## 단계별 실습
1. `Player` 클래스를 만들고 체력(`hp`)을 `private`로 선언한다.
2. 생성자 초기화 리스트로 기본값을 설정한다.
3. `Hit`, `GetHp`를 구현하고 출력으로 동작을 확인한다.
4. `const` 함수 누락/추가 시 컴파일 동작 차이를 확인한다.

## 이해 점검 체크리스트
- 접근 지정자 3종의 용도를 설명할 수 있는가?
- 생성자 초기화 리스트를 사용하는 이유를 설명할 수 있는가?
- `const` 멤버 함수의 제약을 예제로 보여줄 수 있는가?

## 5) 연습 문제
1. `Student` 클래스를 만들고 이름/점수/평균 출력 함수를 구현하세요.
2. 점수 입력 유효성 검사(0~100) 로직을 멤버 함수에 넣으세요.

## 셀프 퀴즈
1. 외부에서 직접 접근을 막고 싶은 멤버는 어떤 접근 지정자를 써야 하는가?
2. 상태를 변경하지 않는 함수에 `const`를 붙이는 이유는 무엇인가?

## 정답
1. `private`
2. 함수가 객체 상태를 바꾸지 않음을 컴파일러와 사용자에게 보장하기 위해서

## 확장 과제 (기본/심화)
- 기본: `Inventory` 클래스를 만들고 아이템 추가/삭제 메서드를 구현한다.
- 심화: 상속 구조(`Character` -> `Player`, `Monster`)로 공통/개별 기능을 분리한다.

---

[상위 문서로 돌아가기](./README.md)
