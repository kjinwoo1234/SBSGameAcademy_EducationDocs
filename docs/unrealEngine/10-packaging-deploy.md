# 패키징과 배포

패키징은 프로젝트를 실행 파일로 변환하는 마지막 단계이며, 사전 점검 품질이 결과물 안정성을 좌우합니다.

## 학습 목표
- 패키징 전 필수 점검 항목을 설명할 수 있다.
- 플랫폼별 패키징 흐름을 실행할 수 있다.
- 배포용 빌드와 개발용 빌드를 구분해 관리할 수 있다.

## 핵심
- 사전 점검: `Default Map`, 입력 설정, 프로젝트 아이콘
- 패키징: `Package Project`에서 타깃 플랫폼 선택
- 결과물 관리: 출력 폴더 구조와 빌드 타입 구분

## 단계별 실습
1. 프로젝트 설정에서 시작 맵(Default Map)을 지정한다.
2. 불필요 에셋을 정리하고 참조 오류가 없는지 확인한다.
3. Windows 플랫폼으로 패키징을 실행한다.
4. 생성된 실행 파일을 별도 환경에서 테스트한다.

## 실수 포인트
- 시작 맵 미지정으로 실행 시 검은 화면 또는 빈 레벨이 나온다.
- 개발 빌드를 그대로 배포해 디버그 정보가 노출된다.

## 이해 점검 체크리스트
- 패키징 전 필수 설정을 점검했는가?
- 타깃 플랫폼 빌드를 직접 생성했는가?
- 생성된 결과물을 실제 실행해 검증했는가?

## 배포 전 QA 체크리스트
- 실행 확인: 설치/실행 시 시작 맵이 정상 로드되는가?
- 저장/로드 확인: SaveGame 데이터가 생성/복원되는가?
- 해상도 확인: 1080p/1440p 등 주요 해상도에서 UI가 깨지지 않는가?
- 입력 장치 확인: 키보드/마우스(또는 패드) 입력이 정상 동작하는가?
- 빌드 타입 확인: 배포본은 Shipping 빌드로 생성되었는가?

## 다음 학습 추천
- [Unreal Engine 자습 자료](./README.md)

## 셀프 퀴즈
1. 게임 시작 시 로드되는 맵을 지정하는 설정은 무엇인가?
2. 개발용 빌드와 배포용 빌드를 구분해야 하는 이유는 무엇인가?

## 정답
1. Default Map
2. 디버그 정보/성능/보안 측면에서 배포 요구사항이 다르기 때문

## 확장 과제 (기본/심화)
- 기본: Development/Shipping 빌드를 각각 생성해 실행 로그/성능 차이를 비교한다.
- 심화: 배포 전 QA 결과(해상도, 입력 장치, 저장/로드)를 표 형태로 기록하는 릴리스 템플릿을 만든다.

---

[상위 문서로 돌아가기](./README.md)
